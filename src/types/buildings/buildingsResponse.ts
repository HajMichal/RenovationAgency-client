export interface BuildingsResponse {
  id: number;
  address: string;
  city: string;
  date: string;
  description: string;
  estimatedArea: string;
  estimatedCost: string;
  title: string;
  zipcode: string;
}
